# 소비나침반 v2.1 후속 에이전트 시작 지시문

첨부/지정한 소비나침반 디자인·API 계약 패키지를 기존 프로젝트에 병합하고 남은 연동 검증을 진행하라.

1. 기존 저장소 지침을 보존하고 이 패키지의 AGENTS.md, AGENT_STATUS.json, BLOCKERS.md부터 읽어라. 이미 반영한 수정은 중복 작성하지 마라.
2. design/DESIGN_SPEC.md와 design/design-preview.html을 시각 기준으로 삼아라. Wanderlog의 계획형 목록·보조 패널 구조는 참고하되 브랜드·이미지·문구·코드를 복제하지 마라.
3. contracts/REVIEW.md의 문제와 원본 계약·OpenAPI를 대조하라. 백엔드 모델과 생성 스크립트를 확보한 뒤 PROPOSED_CHANGES.md의 보완안을 협의·반영하고, 생성 파일을 재생성하라. 백엔드가 없으면 이 작업을 중단하라.
4. 프런트는 서버가 준 가격·잔여 예산·순위만 표시한다. 가상 좌표·사진·출처·금융 연동을 만들지 말고 미제공/미연결 상태를 유지하라.
5. 대상 OS에서 lockfile 기준 의존성을 설치하고 타입 검사·정식 Vite build를 실행하라. 검토용 오프라인 HTML 생성 성공을 배포 빌드 성공으로 보고하지 마라.
6. 가상 UI 테스트와 실제 HTTP/CORS·계산·네이티브 저장소·브라우저 검증을 분리해 실행·기록하라. 검사하지 않은 항목은 NOT_RUN이다.
7. 필수 정보·권한·구현이 부족하면 해당 작업 및 의존 작업을 중단하고 독립 작업만 진행하라. 가능한 작업이 끝나면 문제·시도·필요 파일·재개 조건을 구체적으로 사용자에게 재요청하라. API 키·비밀값을 대화로 요청하지 마라.
8. 종료 전에 진행 상태와 재개 위치를 저장하라. 미해결 필수 검증이 있는 상태에서 전체 완료를 선언하지 마라.

현재 필요한 핵심 입력: backend/ 디렉터리, scripts/export_openapi.py, 백엔드 의존성 파일·실행 방법·비밀값 없는 설정 예시. 현재 UI 검증은 고정 대역 기반으로만 통과했다.
