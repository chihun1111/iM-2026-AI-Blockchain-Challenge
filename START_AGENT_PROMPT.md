# 소비나침반 개발 에이전트 최초 지시문

이 프로젝트의 `AGENTS.md`를 먼저 읽고 `SPEC.md`, `TASKS.md`, `config/policy.v2.json`, `schemas/`, `tests/ACCEPTANCE.md`를 기준으로 소비나침반 프로토타입을 구현하라.

기본 목표는 `P0_DEMO`다. 기존 코드와 미커밋 변경을 먼저 확인하고 임의로 덮어쓰거나 초기화하지 마라. 기존 저장소가 없으면 현재 승인된 작업 디렉터리에 프로젝트를 생성하라. 기존 AGENTS가 있으면 보존하고 이 패키지와 충돌하는 지시는 보고하라. 짧은 시작 계획 후 T00부터 의존관계 순서로 진행하라.

P0에서는 제공된 가상 데이터, 규칙 기반 조건 분석, 템플릿 설명, 목록형 화면을 사용한다. 외부 키가 없어도 P0를 구현할 수 있으나 UI와 보고서에 외부 AI·실지도·실데이터 미연결을 명시하라. 실제 데이터 분석·AI 연결이 완료된 것처럼 표현하지 마라.

막히거나 정보가 부족할 때 다음을 준수하라.
- 비용·권한·비밀정보·기존 데이터 훼손 위험은 즉시 전체 중단하고 필요한 정보나 승인을 요청하라.
- 필수 입력·정책·스키마·환경이 없거나 필수 검증이 실패하면 해당 작업과 의존 작업을 중단하라. 독립적으로 안전하게 할 수 있는 작업만 진행하라.
- 안전한 독립 작업까지 끝났으면 사용자에게 막힌 이유, 영향받는 작업, 시도한 내용과 결과, 필요한 파일·필드·설정, 재개 조건을 구체적으로 다시 요청하라.
- 선택 연동이 미확보이고 P0의 사전 명시된 대체 경로가 있으면 P0를 계속하되, 종료 보고에 미연결 항목과 후속 요청을 반드시 포함하라.
- 승인 범위의 단순 오류는 서로 다른 수정 최대 2회까지 시도하라. 같은 실패를 이유 없이 반복하거나 실패를 숨기지 마라.

각 작업 완료·중단 시 `AGENT_STATUS.json`, `BLOCKERS.md`, `DECISIONS.md`, 검증 보고를 갱신하라. 실제 실행하지 않은 테스트를 PASS로 쓰거나 실패 테스트를 삭제해 완료 처리하지 마라. 세션이 종료되기 전 체크포인트와 구체적인 재개 지시를 남겨라. 사용자 응답 없이 나중에 자동 재개한다고 약속하지 마라.

종료 시 `BLOCKED`, `PARTIAL_REVIEW_REQUIRED`, `DONE_WITH_FOLLOWUPS`, `DONE` 중 정확한 상태를 사용하라. 실제 완료한 기능, 실제 실행한 명령과 테스트, 미완료·미검증 항목, 사용자에게 필요한 입력, 다음 재개 작업을 보고하라. 미해결 필수 기능이 있으면 전체 완료라고 선언하지 마라.
