# 실행 작업표

## 의존관계

T00 → T01 → T02 → T03 → T05 → T06 → T07 → T08

T01 → T04 → T05

X01은 T04·T05 이후, X02는 T06 이후, X03은 T02·T03 이후 각각 권한·입력 확인 후 진행한다. 단일 에이전트는 T00부터 번호순으로 해도 된다.

## 필수 작업

| ID | 산출물 | 게이트·금지 사항 |
|---|---|---|
| T00 | ENVIRONMENT, 기존 변경 기록, 적용 범위·권한 확인 | 기존 파일 무단 덮어쓰기 금지. 키 값 노출 금지. |
| T01 | frontend/backend 골격, 설정 로더, health/meta, 타입 모델, 실행·검증 스크립트 | 외부 키 없이 P0 기동. 잘못된 설정은 명확히 실패. 설치 버전 고정. |
| T02 | SQLite 테이블·외래키·UNIQUE, seed, 스냅샷, 품질 리포트 | 원본 JSON 파일 수정 없이 적재. 재적재 멱등성. 40/40/768행 확인. |
| T03 | budget/filter/ranking/evidence 도메인 서비스와 단위 테스트 | UI 하드코딩 금지. 예산 1원 초과, 결측, 동점, 근거 검증 통과. |
| T04 | 규칙 파서, 확인 메타데이터, Draft/Confirmed 모델, 30문장 평가 | 모호한 인원·예산의 무근거 확정 0건. 실패 시 수동 폼 경로. |
| T05 | 명세의 6개 API와 에러 envelope, OpenAPI 내보내기, 계약 테스트 | source·snapshot·policy·mode 일관성. 서버 금액 재계산. |
| T06 | S01~S05, 저장, 비교, 상태·에러·접근성·반응형 | 다중 대안 금액 합산 금지. 기능 없는 버튼을 작동한다고 표현 금지. |
| T07 | 전체 테스트·빌드·UI 검수·보안·회귀 결과 | 미실행은 NOT_RUN. 브라우저 없으면 UI 게이트 미완료. |
| T08 | README 실행 절차, 시연 절차, 최종 상태·보고·재요청 | 실제 검증한 명령만 성공 사례로 기재. 미연동과 후속 입력 요청. |

## 선택 연동

| ID | 필요한 입력 | 검증 후에만 완료 |
|---|---|---|
| X01 LLM | 공급자, 모델, 서버 키, 텍스트 전송 범위, 비용 승인 | 실호출 1건 이상, 모호한 요청·스키마 오류·timeout·권한 거부 테스트, 규칙 파서와 성능 분리 보고 |
| X02 지도 | 실제 유효 좌표와 사용권, 공개용 JS 키, 허용 도메인 | 실제 지도·마커·목록 동기화와 실패 대체. 가상점포 길찾기 없음 |
| X03 실데이터 | 데이터 계약·사전·샘플·지역/업종/시간 매핑·표본 범위 | real 전용 적재·품질·스냅샷·출처·삭제 정책, real/demo 혼합 차단 |

실제 금융 결제·개인 금융내역·리뷰 수집·배달 거래·다중 방문 코스는 X01~X03에 자동 포함하지 않는다.

## 구현 에이전트가 만들어 검증할 명령 계약

아래 명령과 파일은 **요구 산출물이며 현재 패키지에 앱 스크립트가 이미 있다는 뜻이 아니다.** 에이전트는 현재 OS와 저장소에 맞게 구현하고 README에 정확한 명령을 남긴다.

```text
python scripts/seed_demo.py --check
python scripts/seed_demo.py
python scripts/dev.py
python scripts/verify.py --all
python scripts/export_openapi.py
```

seed는 데모 DB만 대상으로 하고 운영 DB에는 거부한다. dev는 localhost에만 바인딩하고 서버·웹의 종료를 함께 처리한다. verify는 실패 시 비정상 종료 코드와 보고서를 남긴다. Python lockfile·frontend package-lock.json, 필요한 브라우저 설치 절차를 명시한다. 플랫폼 때문에 명령 이름을 바꾸면 원래 계약과 대응표를 README에 기록한다.

## 작업별 체크포인트

각 T/X 작업은 AGENT_STATUS에서 시작 시 IN_PROGRESS, 게이트 통과 후 DONE, 중단 시 BLOCKED로 갱신한다. 증거 배열에는 실제 파일 경로를 넣는다. 빈 evidence로 DONE을 쓰지 않는다. 선택 연동 미승인 상태를 DONE으로 쓰지 않는다.
