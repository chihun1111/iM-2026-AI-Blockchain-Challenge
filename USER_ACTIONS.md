# 사용자가 준비할 프로토타입 입력

현재 필요한 것은 아래 세 묶음이다. API 키 값은 지금 필요하지 않다.

## 1. 점포별 현재 메뉴 근거

국일따로국밥, 개정 본점, 마산설렁탕 각각에 대해 다음을 준비한다.

- 지점 주소와 메뉴명
- 현재 가격, 제공 단위, 최소 주문 수량, 필수비용
- 확인일과 확인자 역할
- 분석·화면 표시·시연에 사용할 수 있는 근거
- 공개 URL 또는 로컬 증빙 파일
- 메뉴판 사진을 외부에 재게시할 경우 촬영·재게시 권한

개인 이름·전화번호는 공개 JSON에 넣지 않는다. 필수비용이 없음을 확인했다면 `0`, 모르면 `null`로 둔다.

## 2. 공공 점포 원본과 카드 집계

- 소상공인시장진흥공단 상가(상권)정보_20260630 대구 원본과 데이터 사전
- 3개 점포를 찾을 수 있는 공공 점포 ID·상호·업종코드/명·주소·위경도
- 신한카드 `지역별매출및이용고객정보` 승인 파일과 데이터 사전·코드표
- 공간코드, 한식 업종코드, 실제 관측 기간, 시간/금액/건수 단위, 측정/추정 방식
- 결측·비공개값 의미
- 분석·반출·코드/데이터 ZIP·화면/영상·공개 웹 각각의 허용 여부

비공개 파일은 채팅에 붙이지 않고 로컬의 접근 제한 경로에 둔다. 원거래·개인 식별자는 필요하지 않다.

## 3. 데이터 API 통과 후 Gemini 승인

실제 데이터 API가 통과하면 `config/gemini_approval_plan.json`의 다음 항목만 결정한다.

- 승인자와 승인 시각
- `gemini-3.5-flash-lite` 사용 승인
- 비식별 짧은 소비조건 문장만 전송하는 범위
- 프로젝트 총 한도와 일 한도

그 다음 서버 환경에만 `GEMINI_API_KEY`를 설정한다. 카드 자료·메뉴판·점포 DB·리뷰·실명·연락처·계좌·정밀 GPS·대화 전체는 Gemini에 보내지 않는다.
