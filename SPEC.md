# 소비나침반 개발 에이전트 실행 명세 v2.0

> 이 문서는 설계·검수 기준이다. 실제 앱 구현 및 테스트는 후속 개발 작업이다.

## 01. 소비나침반 | 개발 에이전트 실행 명세

팀 기적의 인큐4 · 버전 2.0 · 작성일 2026.09.19 · 상태: 구현 전 상세 설계 및 인계 패키지

> **핵심 목표**
> 사용자의 예산·목적·인원·방문 시각을 확인하고, 예산에 맞는 소비처를 추천한 뒤 추천 근거와 예상 지출을 함께 보여준다. “인기 있는 곳”과 “나에게 적합한 곳”을 구분한다.

### 이 문서의 역할

기존 A4 5쪽 제안 요약서와 별개의 개발 명세다. 에이전트가 임의로 기능을 줄이거나 없는 데이터를 꾸며내지 않도록 구현 범위, 계산 규칙, 데이터 계약, 검수 항목과 중단·재요청 절차를 고정한다. 제출용 요약서에는 개발 후 실제 구현·확인된 내용만 반영한다.

| 구분 | v2.0에서 구체화한 내용 |
| --- | --- |
| 제품 설계 | 화면별 동작·상태·예외, 후보 비교와 다중 방문 합산의 구분, 저장·삭제 정책 |
| 데이터·알고리즘 | 40개 가상 점포·768개 집계 행 제공, 출처 추적, 가격 상한, 결측·시효·비교군·동점 처리 |
| 구현 계약 | 설정 파일, 조건 JSON Schema, API 계약, 테스트 사례, 단계별 의존관계 |
| 에이전트 통제 | 장애 등급, 제한된 재시도, 전체/부분 중단, 체크포인트, 사용자 재요청·재개 형식 |

### 기본 실행 목표와 완료의 의미

기본 목표는 P0_DEMO다. 외부 계정 없이 재현 가능한 로컬 시연을 먼저 완성한다. P1_INTEGRATED는 사용권·접근권한·비용 승인이 확인된 실제 데이터, LLM, 지도 연결 단계다. P0 완료는 실제 금융 데이터 분석이나 실제 AI 연결 완료를 뜻하지 않는다.

> **가장 중요한 실행 규칙**
> 추가 정보가 없으면 해당 작업을 BLOCKED로 남긴다. 안전하게 독립 진행할 작업만 계속한다. 필수 작업이 남아 있으면 전체 완료라고 보고하지 않는다. 종료 시 필요한 정보와 재개 조건을 구체적으로 다시 요청한다.

읽는 순서: AGENTS.md → SPEC.md → TASKS.md → config/policy.v2.json → schemas/ → tests/ACCEPTANCE.md. 새 사용자 지시로 범위가 변경되면 변경 기록을 먼저 남긴다. 설정과 본문이 충돌하면 편한 쪽을 선택하지 말고 정책 충돌로 처리한다.

## 02. 제품 범위와 기본 결정

### P0·P1·후속 범위

| 범위 | 반드시 제공할 결과 | 완료 표현 |
| --- | --- | --- |
| P0 필수 | 동성로·수성못, 음식점·카페, 1~6명, 모바일 우선 5개 화면, 규칙 기반 조건 해석, 추천·예산·근거·저장·예외 처리 | 로컬 가상 데이터 시연 |
| P1 선택 연동 | LLM 조건 해석, 실지도, 승인된 실제 데이터 수집·검증. 독립 분기 X01~X03으로 관리 | 연동별 검증 결과를 구분 |
| 후속 확장 | iM샵·대구로페이, 실제 결제·예약, 금융거래 내역, 개인화 학습, 배달·리뷰 수집, 식사+카페 동선 최적화 | 기획 또는 미착수 |

### 재질문 없이 사용할 P0 기본값

비로그인·로컬 실행·한국어·원화 정수·Asia/Seoul을 사용한다. 데이터는 demo, 파서는 rules, 설명은 template, 지도는 off다. 서버에서 추천과 금액을 계산한다. 외부 서비스 호출·유료 호출·공개 배포는 기본 비활성이다. 패키지 설치는 실행 환경의 도구 권한 안에서만 수행한다.

추천은 최대 5개, 대안 비교는 최대 3개, 저장은 최대 20개다. 세션 메모리에 검색 상태를 두고 원문·정밀 위치는 기본 영구 저장하지 않는다. 검색 결과가 없어도 지역·예산·필수 분위기를 자동으로 완화하지 않는다.

### 변경 승인 경계

| 에이전트 자율 진행 가능 | 사용자 확인 후 진행 |
| --- | --- |
| 구현 세부 파일명, 컴포넌트 분리, 테스트 추가, 승인 범위 내 버그 수정 | 필수 기능 삭제, 예산 초과 허용, 실제/가상 데이터 혼합, 계산식·가중치 변경 |
| 지도 미연결 시 목록 유지, LLM 미연결 시 규칙 파서 표시 | 유료 호출, 외부 전송 대상 추가, 실데이터 수집, 공개 배포·원격 push |
| 빈 저장소 안에서 새 프로젝트 구성, 격리된 데모 DB 초기화 | 기존 사용자 파일 덮어쓰기·삭제, 운영 DB 초기화, 인증·보안 장치 우회 |

> **실제 서비스 가설은 아직 미검증**
> 추천 정확도 향상, 소비 절감, 소상공인 매출 증가, 광고 편향 완화는 검증할 가설이다. 합성 데이터와 자체 시연만으로 효과를 입증했다고 표현하지 않는다.

## 03. 요청 해석과 조건 확정

### 대표 사용자 흐름

S01 입력 → S02 조건 확인 → S03 추천 → S04 근거 확인 → S05 대안 예산 비교. 입력 예: “동성로에서 친구랑 저녁 먹을 건데 인당 2만 원 안 넘고 사람들이 많이 이용하는 곳.”

| 필드 | 규칙 |
| --- | --- |
| region_id / category | dongseongro·suseongmot / restaurant·cafe. 지원 범위 밖이면 확정하지 않고 선택 요청. |
| party_size | 정수 1~6. “친구랑”은 2명 제안값으로 표시하되 추정 상태다. “우리”는 인원을 비워 둔다. |
| budget | type=per_person 또는 total, amount=1~10,000,000 정수, currency=KRW. “2만원대”는 정확한 상한으로 확정하지 않는다. |
| visit_at | 시간대 포함 ISO 8601. 기준시각부터 30일 이내. “오늘·내일”은 기준 날짜를 함께 표시한다. “저녁”만 있으면 시각 선택을 요구한다. |
| purpose / atmosphere | purpose는 meal·cafe·date·friends. 분위기 선호와 필수 분위기를 분리하고 필수 태그는 선호 태그의 부분집합이어야 한다. |
| priority_profile / origin | balanced·activity_focus만 허용. P0는 origin=null이고 거리 정렬을 제공하지 않는다. |
| confirmed | S02에서 사용자가 내용을 확인하고 실행해야 true. 클라이언트 값만 믿지 않고 서버가 재검증한다. |

### 파서 응답과 확인 상태

파서는 draft, field_meta, missing_fields, confirmation_required를 반환한다. 각 필드는 explicit·inferred·default·missing으로 구분한다. 추정된 인원·예산 단위·방문 시각은 S02에 표시한다. 확인 이후 값을 수정하면 기존 추천을 폐기하고 다시 조회한다.

규칙 파서는 숫자·한글 수량, “인당/1인/총/전체”, 지역 별칭과 업종 사전을 처리한다. 불확실한 표현을 무리하게 확정하지 않고 해당 필드를 폼으로 보완한다. 원문 500자 초과는 입력 오류다. 빈 입력은 예시와 수동 입력을 보여준다.

> **LLM을 붙여도 바뀌지 않는 책임**
> LLM 출력은 검증 전 입력이다. 정해진 JSON 구조만 허용하고 금액·총예산·순위는 서버에서 산출한다. 외부 AI 미연결 상태를 “AI 분석 완료”로 표시하지 않는다. 조건 스키마는 schemas/conditions.schema.json이 기준이다. [R2]

## 04. 화면별 구현·상태 명세

| 화면 | 핵심 정보와 동작 | 실패·빈 상태 |
| --- | --- | --- |
| S01 / | 자연어 입력·예시·지역 선택·수동 입력. 시연 배너와 기준시각 고정 표시. 분석 중 중복 제출 방지. | 파서 오류면 S02 수동 폼. 사용자가 입력한 원문을 보존. |
| S02 /conditions | 인원, 예산 단위·금액, 날짜·시각, 목적, 분위기 필수 여부 수정. 총예산 미리보기. | 필드 옆 오류 표시. 미확정 상태에서 추천 요청 금지. |
| S03 /results | 최대 5개, 1인·총예상금액, 추천 근거 2~3개, 가격 확인일, 상권 지표 구분. 가격순·추천순. | 결과 0개와 정보 부족을 구분. 조건 완화안은 클릭 전 적용하지 않음. |
| S04 /places/:id | 메뉴 구성·제외 비용, 시간 정보, 근거의 출처·기간·집계 단위, 8주 추세 표, 저장·비교. | 없는 ID는 안내와 복귀. 가상 장소 길찾기 비활성. |
| S05 /budget | 최대 3개를 대안으로 비교. 인원별 예상 상한·잔여금액, 삭제·선택 변경. 저장 목록도 같은 화면의 탭으로 제공. | 후보가 없으면 결과로 이동. 스냅샷 변경 시 재계산 요청. |

### 카드와 근거 패널

카드: [가상 점포·시연 데이터] 데모 동성로 식당 1-1 / 1인 15,000~18,000원 / 2인 30,000~36,000원 / 잔여 4,000~10,000원 / “예산 상한 충족” + “저녁 식사 목적 일치”. 상권 활동을 표시할 때는 “주변 상권 집계”라고 명시한다.

상세의 온라인 반응과 오프라인 행동은 별도 구획이다. P0 온라인 반응은 “리뷰 데이터 미연결”로 둔다. 높은 결제량에서 조용함·단골·주민 방문·실시간 혼잡을 추론하지 않는다.

### 공통 UI 계약

기본 폭 360·390·768·1280px에서 가로 넘침 없이 검수한다. 본문 16px 이상, 주요 조작 영역 44px 이상을 내부 목표로 둔다. 폼 label·키보드 탐색·포커스·상태 알림·텍스트 오류를 제공한다. 차트와 같은 정보를 표로도 제공한다. 색상만으로 상태를 구분하지 않는다.

이전 응답이 늦게 도착해도 최신 조건의 결과를 덮지 않도록 요청 ID와 취소 처리를 사용한다. 목록·지도·상세는 동일한 snapshot_id를 쓴다. localStorage에는 place_id·snapshot_id·saved_at만 저장하고 전체 삭제를 제공한다. 검색 원문·좌표는 저장하지 않는다.

## 05. 기술 구성과 실행 구조

### 제안 스택와 역할

| 계층 | 선택 | 역할 |
| --- | --- | --- |
| 웹 | React + TypeScript + Vite | 5개 화면·폼·카드·비교·공통 상태. 서버 계산 결과를 표시. |
| 서버 | Python + FastAPI + Pydantic | 조건·예산·추천·근거 API, 입력 검증, OpenAPI 내보내기. [R2][R3] |
| 저장·집계 | SQLite + Python 표준 모듈, 필요 시 pandas | 장소·가격·집계·출처·스냅샷. 수집과 요청 처리를 분리. |
| 테스트 | pytest / Vitest / Playwright | 계산·계약·컴포넌트·사용자 흐름 검증. [R3][R4] |
| 선택 어댑터 | LLM / Kakao Maps / 공공 데이터 | 동일 인터페이스 뒤에 배치. 키와 사용 승인 후에만 실호출. [R1][R5] |

브라우저 → API → 도메인 서비스(조건·추천·예산·근거) → 저장소. 별도 수집 작업은 원자료 → 정규화 → 품질 검사 → 불변 스냅샷 → 활성 버전 교체 순으로 진행한다. 사용자의 검색 요청 안에서 전체 데이터를 다시 수집하지 않는다.

### 구현 에이전트가 생성할 구조

```text
frontend/src/{pages,components,api,state}
backend/app/{api,domain,adapters,models,repositories}
backend/tests/ · frontend/tests/ · tests/e2e/
scripts/ · config/ · data/demo/ · schemas/ · reports/
```

계약 인터페이스: QueryParser.parse → ParseResult, PlaceRepository.list_candidates → 장소 후보, MetricsRepository.get_features → 집계 특징, RankingService.rank → 정렬, EvidenceRenderer.render → 근거 문장. AI·지도·데이터 공급자를 UI나 계산 모듈에 직접 결합하지 않는다.

### 환경·버전 관리

기존 저장소와 lockfile을 우선 확인한다. 신규 구성은 Python 3.12 계열을 제안 기준으로 하고, Node는 선택한 Vite가 지원하는 설치 가능한 LTS 계열을 공식 요구사항과 대조한다. 검증된 정확한 버전·OS·명령을 reports/ENVIRONMENT.md와 lockfile에 기록한다. “최신 버전”이라는 이유로 일괄 업그레이드하지 않는다.

설정은 config/policy.v2.json을 로드하고 환경변수는 배포·접속·비밀값을 담당한다. 예산식·가중치·임계값을 UI에서 중복 하드코딩하지 않는다. 서버 비밀키는 VITE_ 변수에 넣지 않는다. Vite는 해당 접두어 변수를 클라이언트 코드에 노출한다. [R6]

## 06. 데이터 모델과 제공 파일

### 바로 사용할 수 있는 가상 자료

data/demo/에는 출처 2개, 가상 구역 8개, 점포 40개(지역별 20개), 가격 구성 40개, 주간 집계 768개를 제공한다. 집계는 8구역 × 2업종 × 2요일군 × 3시간대 × 8주다. 2026.07.20~09.13의 완료된 주간이며 기준시각은 2026.09.19 18:00이다.

| 자료 | 필수 키·필드 | 주의 |
| --- | --- | --- |
| sources | source_id, kind, provider, permission_status, observed_at, scope, is_mock | 출처는 값에 연결. 사용권 확인 여부를 구분. |
| areas | area_id, region_id, boundary_type, centroid | 가상 ID는 실제 행정구역 코드가 아님. |
| places | place_id, area_id, category, purpose_tags, atmosphere_tags, opening_weekly, source_id | 실제 주소·좌표 없음. 인원 1~6명 지원. |
| price_bundles | bundle_id, place_id, unit_min/max, fixed_fee_min/max, included, excluded, observed_at | 1인 구성 기준. 실제 개별 메뉴판을 확보한 것으로 표현 금지. |
| area_metrics | area_id, category, day_group, time_bucket, week_start/end, txn_count, merchant_count, source_id | 상권 집계. 점포 매출 또는 주민 이용 데이터가 아님. |
| manifest | snapshot_id, data_mode, reference_now, 기간, 행 수, 파일별 sha256 | 재현·중복 적재 방지와 테스트 고정 기준. |

### DB와 데이터 관계

place → area, price_bundle → place, 집계 → area+category, 모든 수치 → source를 연결한다. 데이터 테이블은 snapshot_id를 포함하고 기본키·외래키·집계 복합 UNIQUE를 선언한다. 원천 파일 행 번호·해시를 기록해 잘못된 값을 역추적할 수 있게 한다.

주간 집계의 가맹점 수는 40개 추천 후보와 별도의 가상 관측 모집단이다. 추천 목록의 가상 점포별 소비를 합산해서 생성한 자료가 아니며, 임의로 그 관계를 만들어 설명하지 않는다.

> **실제 데이터가 들어왔을 때**
> 실제 상가정보 API는 상가업소번호·상호·주소·업종·좌표 등의 기초 후보로 검토한다. 해당 자료가 있다는 이유로 메뉴가격·점포 결제액·방문객 수까지 확보됐다고 가정하지 않는다. 카드·인구·리뷰 자료는 각각 사용권과 필드 정의를 다시 확인한다. [R1]

## 07. 결합·신선도·근거 검증

### 적재 게이트

수집 → 형식 검사 → 고유키·기간·공간 단위 검사 → 음수·결측·표본 검사 → 출처·권한 검사 → 스냅샷 생성 순으로 진행한다. 적재는 임시 DB에서 검증한 뒤 원자적으로 교체한다. 재실행해도 동일 키의 중복 행이 생기지 않아야 한다.

실데이터는 제공기관 코드나 검증된 공간 경계를 사용한다. 동 이름이 비슷하다는 이유로 조인하지 않는다. area_id 매핑·좌표계·업종 매핑·시간 집계 의미가 불명확하면 실데이터 적재 분기만 중단하고 자료 사전과 샘플을 요청한다. 실패한 자료를 가상 값으로 몰래 보충하지 않는다.

| 검사 | 초기 정책과 처리 |
| --- | --- |
| 가격·영업·분위기 | 확인일부터 각각 30·14·90일. 초과 또는 기준일 누락은 미확인. 미래 관측시각은 오류. |
| 상권 집계 | 마지막 완료 주 종료일부터 45일 이내. 같은 정의의 연속 8주 필요. 결측 주를 0으로 채우지 않음. |
| 소수 집계 | 주간 결제 20건 미만 또는 관측 가맹점 5개 미만이면 비공개·미산정. 원자료의 더 엄격한 제한을 우선. |
| 비교 가능성 | 동일 출처·업종·요일군·시간대·기간·집계 정의. 비교군은 점포 수가 아니라 서로 다른 구역 5개 이상. |
| 관측 범위 변화 | 직전 4주 대비 최근 4주 평균 가맹점 수 변화가 20% 초과하면 추세는 미산정. |
| 실제/가상 혼합 | 한 응답·스냅샷에서 혼합 금지. real 실패를 demo로 자동 전환하지 않음. |

위 일수·표본 수·변화율은 개발용 초기 정책값이다. 법적 익명성·대표성·통계적 유의성을 보장하는 기준이 아니다. 실제 데이터 계약과 검증 결과에 따라 승인 후 버전을 바꾼다.

### 근거 레코드

각 근거는 evidence_id, place_id 또는 area_id, source_id, snapshot_id, claim_type, value, unit, observed_at, 기간, 집계 단위, is_mock을 갖는다. 주장은 PRICE_RANGE·BUDGET_FIT·PURPOSE_MATCH·ATMOSPHERE_TAG·AREA_ACTIVITY·AREA_TREND 등 허용된 유형만 사용한다.

P0 설명은 템플릿으로 생성한다. P1에서도 자유 문장만 검사해 신뢰하지 않고 구조화된 claim_type·evidence_id와 숫자를 검증한 후 서버 템플릿으로 표시한다. 연결되지 않은 근거 ID나 값 불일치는 설명을 숨기고 기록한다. 신뢰도 백분율은 별도 보정 검증 전 표시하지 않는다.

## 08. 예산 계산과 비교의 정확한 규칙

### 공통 계산식

```text
N = 확정 인원, A = 입력 예산
B = A × N  (per_person)  또는 A  (total)
L = unit_min × N + fixed_fee_min
U = unit_max × N + fixed_fee_max
잔여 최소 = B - U / 잔여 최대 = B - L
1인 최소 = floor(L / N), 1인 최대 = ceil(U / N)
```

금액은 원화 정수로 다룬다. 확인된 필수 비용은 상한에 포함하고 불확실한 할인은 차감하지 않는다. 모든 구성 가격은 양수이며 min≤max, 고정 비용은 0 이상이어야 한다. 나눗셈 반올림은 표시용이고 예산 판정은 정수 총액으로 한다.

| 예산 상태 | 정의 | 기본 추천 |
| --- | --- | --- |
| fits | 가격이 유효하고 U≤B. 1인 예산이면 1인 최대도 A 이하. | 포함 가능 |
| exceeds | 가격이 유효하지만 U>B. 일부 금액대가 예산 안이어도 초과로 판정. | 제외 |
| unknown | 가격 결측·시효 초과·미확인 필수 비용 등으로 상한을 검증할 수 없음. | 정보 부족 영역 |

### 대표 검산 사례

| 사례 | 입력·계산 | 기대 결과 |
| --- | --- | --- |
| 기본 | 2명 × 1인 예산 20,000원, 구성 15,000~18,000원 | 총 30,000~36,000원 / 잔여 4,000~10,000원 |
| 경계 | 총예산 36,000원, 예상 상한 36,000원 | fits, 잔여 최소 0원 |
| 1원 초과 | 총예산 35,999원, 예상 상한 36,000원 | exceeds, 잔여 최소 -1원 |
| 고정 비용 | 2명 × 18,000원 + 필수 요금 5,000원, 총예산 40,000원 | 상한 41,000원, 초과 1,000원 |

### 대안 비교와 방문 코스는 다르다

S05의 세 장소는 “셋 중 어디를 고를지” 비교하는 대안이다. 세 곳의 금액을 합산하지 않는다. 식사+카페처럼 여러 곳을 실제 방문하는 코스 합산은 별도 후속 범위다. 화면에 “각 장소를 선택했을 때의 예상 지출”이라고 명시한다.

클라이언트가 보낸 가격·잔여 예산을 신뢰하지 않는다. 서버는 place_id·bundle_id·snapshot_id를 검증하고 다시 계산한다. 과거 스냅샷을 찾을 수 없으면 409로 갱신을 요청한다. 음수 잔여 금액을 0으로 바꿔 초과를 숨기지 않는다.

## 09. 추천·정규화·결측 처리

### 필터를 먼저, 순위는 나중에

필수 지역·업종·인원·예산 상한·영업 여부·필수 분위기를 확인한다. 이미 휴무이거나 조건을 위반한 후보는 제외한다. 가격·영업·필수 태그가 미확인이면 정보 부족 후보로 분리한다. 방문 시점의 개점 여부만 다루며 좌석·대기·예약 가능성을 보장하지 않는다.

| 요소 | balanced / activity_focus | 산식 또는 정의 |
| --- | --- | --- |
| 예산 | 0.30 / 0.25 | clamp((B-U)/B, 0, 1). 더 저렴하다는 사실을 품질 우수로 해석하지 않음. |
| 목적 | 0.20 / 0.15 | 요청 purpose가 출처 있는 purpose_tags에 있으면 1, 아니면 0. |
| 분위기 | 0.15 / 0.10 | 일치 선호 태그 수 / 요청 선호 태그 수. 선호가 없으면 공통 제외. |
| 거리 | 0.10 / 0.10 | max(0, 1-d/r). 실제 기준 좌표와 후보 좌표가 있을 때만 사용. P0 제외. |
| 상권 활동 | 0.15 / 0.25 | 주별 x=txn_count/merchant_count, 최근 4주 x 평균의 비교군 백분위. |
| 일관성 | 0.05 / 0.10 | 1 / (1 + 8주 x의 모집단 표준편차 / 평균). 평균 0이면 미산정. |
| 추세 | 0.05 / 0.05 | 최근 4주 x 평균 / 직전 4주 x 평균 - 1의 비교군 백분위. 분모 0·결측·관측범위 변화 시 미산정. |

백분위는 (작은 값 개수 + 0.5×같은 값 개수) / 구역 수로 계산한다. 비교군은 동일 스냅샷의 같은 출처·업종·요일군·시간대·완료 기간을 가진 구역 전체이며, 사용자가 선택한 지역의 후보만으로 다시 만들지 않는다. 모든 값이 같으면 0.5다.

요청에서 불필요하거나 전체적으로 확보되지 않은 지표는 후보 공통으로 제외하고 나머지 가중치를 합계 1로 재정규화한다. 한 후보에만 활성 지표가 없으면 그 후보만 별도 “근거 부족” 그룹으로 보내며, 결측을 0이나 평균으로 대체하거나 후보별로 가중치를 유리하게 바꾸지 않는다.

점수는 100×가중합을 소수점 6자리로 반올림한 뒤 점수 내림차순 → 예상 상한 오름차순 → place_id 오름차순으로 정렬한다. 최종 사용자에게 점수나 정답 확률 대신 근거를 보여준다. 같은 조건·스냅샷·정책 버전이면 순서가 같아야 한다.

> **해석 제한**
> 상권 활동은 관측 가맹점당 주간 결제 활동이다. 단골 비율·실제 주민 비중·현재 혼잡도가 아니다. 방문인구는 별도 맥락으로만 표시하며 결제건수와 직접 더하지 않는다.

## 10. API·응답·오류 계약

| API | 입력 → 출력 |
| --- | --- |
| GET /api/health | 설정 유효성·DB 접근·스냅샷 상태. 선택 어댑터 off는 장애로 오인하지 않음. 비밀값 미노출. |
| GET /api/meta | 지원 지역·업종·태그·data_mode·parser_mode·지도 상태·reference_now·snapshot_id·policy_version. |
| POST /api/parse-query | {text} → draft, field_meta, missing_fields, confirmation_required, parser_mode. |
| POST /api/recommendations | {conditions, snapshot_id} → items 최대 5개, insufficient_data_items, excluded_counts, active_features, warnings. |
| GET /api/places/{id} | snapshot_id 질의값 → 장소·가격·시간·근거·상권 시계열. ID 미존재는 404. |
| POST /api/budget/estimate | {conditions, snapshot_id, place_ids} → comparison_mode=alternatives, total_budget, 장소별 비용·잔여·상태·가정. |

### 응답 공통 필드

request_id, snapshot_id, policy_version, data_mode, reference_now, warnings를 포함한다. 추천 item은 place_id, cost_min/max, budget_status, evidence_ids, opening_status, is_mock을 포함한다. data_as_of는 자료별로 표시한다. 점수 상세는 내부 테스트 전용이며 브라우저에 불필요하게 내보내지 않는다.

FastAPI 모델에서 OpenAPI를 생성하되 배포 시점 명세를 schemas/openapi.generated.json으로 보관한다. 구현 에이전트가 생성하는 파일이며 현재 패키지가 실행 가능한 API 서버를 포함하는 것은 아니다. Pydantic JSON Schema와 API 예시를 계약 테스트에 사용한다. [R2]

| 응답 | 의미와 복구 |
| --- | --- |
| 200 + items=[] | 정상 검색이지만 조건에 맞는 후보 없음. 적합 후보를 꾸며내지 않음. |
| 413 / 422 | 요청 16KB 초과 / 조건 형식·범위·확인 상태 오류. errors에 필드 경로·수정 안내 제공. |
| 404 / 409 | 장소 없음 / 스냅샷·데이터 모드 불일치. 다시 조회하거나 조건 화면으로 복귀. |
| 503 | 필수 데이터 로딩·서버 의존 실패. 오류 코드·재시도 가능 여부를 반환. demo로 몰래 전환 금지. |

오류 형식: {request_id, error:{code, message, retryable, fields}}. 예상치 못한 내부 오류에는 스택·경로·키를 노출하지 않는다. 입력 저장·결제 같은 변경 API는 P0에 없다. 즐겨찾기는 브라우저 로컬 상태로 관리한다.

## 11. 실패·보안·시간 처리

### 런타임 장애와 개발 장애를 구분

앱 사용 중 외부 API가 실패하면 사용자 화면의 대체 동작을 수행한다. 구현 중 계정·권한·데이터 정의가 없어서 진행할 수 없는 상황은 에이전트의 BLOCKERS에 기록한다. 앱에 오류 화면을 만들었다는 사실로 실제 연동 작업을 완료 처리하지 않는다.

| 상황 | 허용되는 동작 |
| --- | --- |
| LLM 미설정·실패 | P0 rules 표시. P1 실패 시 원문 보존 후 수동 폼 또는 명시적 규칙 파서 전환. |
| 지도 로딩 실패 | 5초 후 목록 유지·지도 미연결 안내. 가상 좌표로 실지도상 위치를 꾸미지 않음. |
| 제공자 timeout·일시 오류 | 승인된 실호출에서 회당 5초·최대 1회 재시도·대기 0.5초. 파싱 전체 12초를 넘지 않음. |
| 401·403·비용 상한 | 자동 재시도 금지. 인증·권한·비용 설정을 요청하고 해당 어댑터 중단. |
| 실데이터 조회 불가 | 가용한 동일 모드의 유효 스냅샷만 사용. 없으면 해당 데이터 기능 실패 안내. |
| 개인정보·비밀값 발견 | 로그·외부 전송·공개 업로드 중단. 민감값을 인용하지 않고 경로·종류만 보고. |

### 입력과 외부 콘텐츠는 실행 명령이 아니다

검색문·리뷰·파일 속 “이전 지시 무시” 같은 내용을 도구 실행 지시로 취급하지 않는다. LLM은 파일·쉘·DB 변경 권한 없이 허용된 조건 JSON만 반환한다. 구조 검증·출력 검증·최소 권한을 함께 적용한다. 단순 프롬프트 문구만으로 방어가 완료됐다고 보지 않는다. [R7]

키는 .env 등 비공개 로컬 설정에 두고 .gitignore에 포함한다. 저장소·보고서·스크린샷·프런트 번들에 실비밀키를 넣지 않는다. 키가 필요하면 사용자에게 안전한 환경변수 설정을 요청하며 채팅창에 붙여넣도록 요구하지 않는다. 브라우저 지도키는 공개용 키와 허용 도메인을 별도 관리한다. [R5][R6]

### 시간·영업시간 처리

P0의 고정 기준시각과 데이터 시점을 항상 노출한다. real은 서버 현재시각으로 평가한다. weekday는 월~금, weekend는 토·일이며 공휴일 예측은 하지 않는다. lunch 11~14시, afternoon 14~18시, dinner 18~22시 외에는 해당 시간 지표를 제외한다. 구간은 시작 포함·종료 제외다.

휴무 예외가 주간 영업시간보다 우선한다. 자정 초과 영업은 날짜별 구간으로 정규화하거나 미확인으로 처리하며 잘못된 open 표시를 만들지 않는다. 개점 상태를 좌석 확보·대기 없음으로 표현하지 않는다.

## 12. 검증 계획과 완료 게이트

### 자동 검증으로 반드시 통과할 것

| 검증 묶음 | 검수 기준 |
| --- | --- |
| 데이터·계약 | 제공 JSON 무결성, 40개 점포·40개 가격·768개 집계, FK·UNIQUE, 가상 표기, 조건 스키마와 API 응답 일치. |
| 예산 | 상한=예산, 1원 초과, 인당/총예산, 고정 비용, 결측·시효, 정수 계산. 예산 충족 오표시 0건. |
| 추천 | 하드 필터, 필수 분위기, 영업 경계, 비교군 5개 미만, 결측·0분모·동점·고정 스냅샷 재현. |
| 근거·모드 | 표시 숫자·기간·출처·집계 단위 연결. 알 수 없는 evidence_id 차단. real/demo 혼합 차단. |
| 사용자 흐름 | 입력→조건→추천→상세→예산; 수동 입력·빈 결과·저장·삭제·뒤로가기·응답 순서 역전. |
| 오류·보안 | 파서 실패·지도 실패·키 누락·권한 실패·프롬프트 주입·비밀키 번들 포함 검사. |

### 평가 방식

tests/ACCEPTANCE.md와 cases.json의 필수 사례는 전부 PASS여야 한다. tests/parse_cases.json 30문장은 요구 필드별 정확 일치와 필요한 재확인 표시를 평가한다. 필드 정답률 90% 이상을 초기 목표로 두되, 인원·예산 단위의 근거 없는 확정은 0건이어야 한다. 별도의 미공개·사용자 작성 문장으로 일반화 성능을 추가 검증한다.

자체 작성한 고정 문장 통과만으로 자연어 이해 성능을 일반화하지 않는다. 규칙 파서와 실제 LLM은 결과를 분리 보고한다. 자체 시연·가상 데이터 검증은 실제 시민·소상공인 효과 검증을 대체하지 않는다.

### 실행 증거와 수동 검수

보고서에는 실제 실행한 명령, 종료 코드, 실행 환경, 시각, 테스트 수, PASS·FAIL·NOT_RUN·SKIP_APPROVED, 로그 경로를 기록한다. 실행하지 않은 테스트는 NOT_RUN이다. 브라우저 도구가 없으면 스크린샷을 꾸며내지 않고 UI 검증을 미실행으로 남겨 PARTIAL_REVIEW_REQUIRED로 보고한다.

360·390·768·1280px 정상 흐름과 에러 화면을 검수한다. 로컬 추천 API는 LLM을 제외한 고정 데이터 조건으로 10회 준비 실행 후 100회 측정하고 p95 500ms 이하를 목표로 보고한다. 환경 의존 목표이며 측정 전 성과가 아니다. 5명 중 4명 이상 독립 수행은 후속 사용성 목표로 별도 기록한다.

> **하드 게이트**
> 필수 예산·추천·데이터·계약·E2E·UI 검수가 실패하거나 미실행이면 P0 전체 완료 선언 금지. 품질 목표 미달을 없애려고 테스트·임계값·기능 범위를 몰래 낮추지 않는다.

## 13. 작업 분해와 단계별 종료 조건

| 작업 | 선행·산출물 | 완료 게이트 |
| --- | --- | --- |
| T00 사전 점검 | 문서·기존 코드·미커밋 변경·런타임·명령 권한 점검. ENVIRONMENT와 DECISIONS 기록. | 빈 저장소/기존 저장소 판단, 안전한 경로 확정. |
| T01 계약·골격 | T00. 모델·설정 로더·health/meta·디렉터리·테스트 명령. | 설정 오류가 드러나고 외부 키 없이 로컬 기동. |
| T02 데이터 적재 | T01. 데모 DB·idempotent seed·스냅샷·품질 리포트. | 중복·키·행 수·해시·가상 표기 검증. |
| T03 계산 엔진 | T02. 예산·필터·정규화·정렬·근거 템플릿. | 경계값·결측·재현·근거 테스트 통과. |
| T04 조건 해석 | T01. 규칙 파서·조건 검증·30개 평가·수동 폼 계약. | 추정값 표시·모호성·스키마 검사 통과. |
| T05 API 통합 | T03+T04. 6개 API·오류·OpenAPI·계약 테스트. | 예시 요청·응답·정상/예외 호출 통과. |
| T06 화면 구현 | T05. 5개 화면·저장·비교·상태·반응형. | 필수 흐름 및 오류 E2E·UI 검수 통과. |
| T07 회귀·보안 | T06. 전체 검증·빌드·번들·출처·실패 시나리오. | 하드 게이트 통과, 증거 보관. |
| T08 인계 | T07. README·실행 명령·시연 절차·상태·블로커·최종 보고. | 새 환경에서 실행 절차 검증, 재요청 명시. |

### 선택 연동 분기

X01 LLM: T04·T05 이후 공급자·모델·키·전송 범위·비용 승인 필요. X02 지도: T06 이후 유효한 실제 장소 좌표·브라우저 지도키·허용 도메인 필요. X03 실제 데이터: T02·T03 이후 사용권·자료 사전·지역/업종/시점 매핑·품질 기준 필요. 분기별 검증 전 실제 연동 완료 표시 금지.

한 에이전트는 의존관계 순서로 수행한다. 병렬 에이전트를 쓰면 frontend·backend·data 소유 범위를 나누고 스키마·정책은 조정자만 변경한다. 작업 시작 전에 대상 파일 소유권과 계약 버전을 확인하고 다른 에이전트의 변경을 덮어쓰지 않는다.

각 작업 완료 시 상태·증거·다음 작업을 저장한다. 실패한 선행 작업의 하위 작업을 완료로 표시하지 않는다. 문서·UI 껍데기처럼 독립 작업을 병행할 수 있어도 최종 통합 검증은 선행 게이트 이후 수행한다.

## 14. 막힐 때: 중단·부분 진행·재요청

| 등급 | 판단 기준 | 행동 |
| --- | --- | --- |
| B0 전체 중단 | 비밀 유출·무단 비용·운영 데이터 훼손 위험, 진행 경로 자체의 권한 부재, 해석에 따라 핵심 안전·승인 범위가 달라지는 충돌 | 위험한 실행 즉시 중단. 민감값 제외 체크포인트 저장 후 구체적 승인·정보 요청. |
| B1 해당 작업 중단 | 필수 스키마·원자료·런타임·접근권한 부족, 필수 계산·테스트 실패, 서로 다른 2회 수정 후 미해결 | 의존 하위 작업 보류. 안전한 독립 작업만 진행. 더 진행할 필수 작업이 없으면 전체 BLOCKED. |
| B2 보류 후 종료 때 요청 | P0에서 외부 LLM·지도·실데이터 미확보, 선택 연동 미승인, 사전 명시된 대체 경로가 있음 | P0 범위의 대체 방식을 명시하고 진행. 미구현 항목은 끝에 재요청. P1을 목표로 지시받았다면 B1로 승격. |
| B3 제한된 자체 해결 | 승인 범위의 오탈자·경로·타입·호환성·테스트 픽스처 문제 | 원인에 맞는 서로 다른 수정 최대 2회. 동일 오류를 수정 없이 반복하지 않음. 초과하면 B1. |

### 중단 판단 순서

① 위험·권한 문제면 B0 → ② 현재 목표의 필수 조건인가 확인 → ③ 이미 정한 안전한 대체 경로가 있는지 확인 → ④ 독립 작업이 남아 있는지 판단 → ⑤ 상태와 재요청을 기록한다. 사용자가 실데이터를 요구했는데 몰래 데모로 바꾸는 것은 대체 경로가 아니다.

### 절대 하지 말 것

API 키·샘플 데이터·결제 혜택을 지어내지 않는다. 미실행을 PASS로 기록하지 않는다. 실패하는 테스트를 지우거나 계산 규칙을 완화해 통과시키지 않는다. 인증 오류에 무한 재시도하지 않는다. 운영 DB나 사용자 코드를 임의 초기화하지 않는다. 요청하지 않은 배포·원격 push·결제를 실행하지 않는다.

> **사용자가 원하는 동작**
> 핵심 정보가 없으면 멈추고 요청한다. 영향이 분리되는 부족 사항은 가능한 범위까지 완료한 뒤 다시 요청한다. 어떤 경우에도 부족 사항을 숨긴 채 끝내지 않으며, 다음 대화에서 이어갈 수 있는 재개 지점을 남긴다.

이 문서의 자체 수정 횟수는 개발 에이전트의 문제 해결 한도다. 앱의 외부 요청 재시도 횟수와 별개다. 비용·삭제·보안·권한 문제에는 자체 수정 한도를 소진하려 하지 말고 바로 중단한다.

## 15. 진행 상태·보고 형식·재개 절차

### 항상 유지할 파일

AGENT_STATUS.json은 작업·검증 상태의 구조화된 기록이다. BLOCKERS.md는 미해결 문제·영향·요청 사항을, DECISIONS.md는 범위와 정책 결정을, reports/TEST_REPORT.md는 실제 검증 증거를, reports/FINAL_REPORT.md는 최종 상태를 기록한다. 값은 관련 작업이 끝날 때 갱신한다.

| 최종 상태 | 사용 조건 |
| --- | --- |
| BLOCKED | 필수 작업이 막혔고 승인된 독립 작업도 더 진행할 수 없음. |
| PARTIAL_REVIEW_REQUIRED | 구현 또는 필수 검증이 일부 남았음. 미실행·실패를 구분. |
| DONE_WITH_FOLLOWUPS | 선언한 P0 등의 목표와 하드 게이트는 충족했으나 실제 연동·사용자 제공 정보 등 후속 요청이 남음. |
| DONE | 선언한 전체 목표의 필수 작업·검증이 완료되고 요청할 미해결 항목이 없음. 계획만 한 선택 기능은 완료로 계산하지 않음. |

### 중단·종료 시 재요청 형식

```text
[상태] BLOCKED 또는 DONE_WITH_FOLLOWUPS 등
[문제 ID / 영향] BLK-001 / X03 실제 데이터 적재
[완료한 내용] 파일·작업 ID·통과한 테스트
[막힌 이유] 관측 가맹점 수 정의와 area_id 대응표 미확보
[시도와 결과] 확인한 자료·명령·오류 요약(비밀값 제외)
[필요한 입력] 데이터 사전, 비식별 샘플, 지역 매핑표
[재개 조건] 필수 필드·사용권 검증 통과
[재개 위치] X03-입력 검증부터; 기존 P0 결과 보존
```

재요청은 “더 알려주세요”처럼 막연하게 하지 않는다. 필요한 파일명·필드·환경변수 이름·허용 형식·왜 필요한지를 적는다. 같은 원인의 요청은 합치고 이미 받은 정보를 다시 묻지 않는다. 핵심 정보가 부족한 상태에서 완성됐다고 결론내리지 않는다.

### 사용자 응답 이후

다음 실행에서 AGENT_STATUS·BLOCKERS·변경 내역을 읽고 입력 충족 여부를 확인한다. 기존 완료 작업을 무조건 다시 만들지 않는다. 차단 원인을 해소한 뒤 해당 작업의 테스트와 영향받는 회귀 테스트를 재실행한다. 세션 한계로 끝날 때도 체크포인트와 재개 요청을 남긴다. 자동으로 나중에 재개하거나 백그라운드에서 완료한다고 약속하지 않는다.

## 16. 인계 산출물과 적용 방법

### 개발 후 인계해야 할 결과

실행 가능한 웹·API 코드, 의존성 lockfile, 비밀값 없는 .env.example, 정책·계약·스냅샷, 단위·계약·E2E 테스트, 실제 화면 검수 기록, 재현 가능한 실행·시드·검증 명령, 시연 절차와 최종 보고를 제공한다. “구현 예정”과 “실행 검증 완료”를 명확히 나눈다.

현재 전달 패키지는 구현 명세·에이전트 규칙·정책·JSON 스키마·가상 데이터·검수 사례·상태 템플릿이다. 앱 코드·실제 외부 연동·앱 테스트 통과 결과를 이미 제공한 것은 아니다. 패키지 정합성 검사는 제품 동작 검증과 구분한다.

### 에이전트 시작 지시

> **START_AGENT_PROMPT.md를 전달**
> 이 폴더의 AGENTS.md를 먼저 읽고 SPEC.md·TASKS.md·정책·테스트 기준으로 P0_DEMO를 구현하라. 막히면 장애 등급에 따라 전체 또는 해당 작업을 중단하고, 독립 작업만 계속하라. 종료 시 실제 완료·미완료·검증 상태·필요한 입력·재개 위치를 반드시 보고하라.

### 공식 참고자료 | 확인일 2026.09.19

| ID | 자료와 적용 범위 |
| --- | --- |
| R1 | 공공데이터포털: 소상공인시장진흥공단 상가(상권)정보 API. 기초 상가 항목 범위 확인. |
| R2 | Pydantic: JSON Schema. 조건 모델·스키마 계약. |
| R3 | FastAPI: Testing. TestClient 기반 API 검증. |
| R4 | Playwright: Assertions. 브라우저 상태 검증. |
| R5 | Kakao 지도 Web API 가이드. 키·도메인 설정. |
| R6 | Vite: Env Variables and Modes. 클라이언트 공개 변수와 비밀값 분리. |
| R7 | OWASP: LLM Prompt Injection Prevention. 최소 권한·입출력 검증. |

원문 주소는 패키지 REFERENCES.md에 수록했다. 가중치·임계값·기능 범위·중단 정책·완료 기준은 본 프로젝트의 설계 결정이며 외부 문헌이 성능을 보장한다는 의미가 아니다. 실연동 시 제공기관의 당시 문서·이용조건·권한을 다시 확인한다.

> **최종 인계 질문의 원칙**
> 선언한 범위는 모두 검증했는가? 가상·실제·미연결을 구분했는가? 미해결 사항이 있으면 사용자가 무엇을 제공해야 어디서 이어갈 수 있는지 구체적으로 적었는가?
